{
  "name": "copilot-agent-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Copilot agent platform monorepo",
  "scripts": {
    "validate:all": "node ./tools/cli/validate-all.js",
    "manifest:generate": "node ./tools/cli/generate-manifest.js",
    "manifest:generate:looka": "node ./tools/cli/generate-manifest.js looka",
    "manifest:generate:helmut": "node ./tools/cli/generate-manifest.js helmut"
  }
}
